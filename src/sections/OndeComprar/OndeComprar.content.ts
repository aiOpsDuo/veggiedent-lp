// Fonte: Copy Deck v2.0, secao 11. [BLOQUEADO] lista de parceiros/marketplaces
// ainda nao confirmada (PRD v1.2, secao 18) — array vazio de proposito, para
// que a ativacao real seja so preencher esta lista, sem mudanca de componente
// (Especificacao Funcional, secao 6.10).
import type { OndeComprarContent } from './OndeComprar.types'

export const ondeComprarContent: OndeComprarContent = {
  heading: 'Onde encontrar Veggiedent',
  intro: 'A Virbac não vende diretamente ao consumidor final. Encontre Veggiedent com os parceiros abaixo.',
  partners: [],
}
